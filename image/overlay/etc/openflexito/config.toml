# openflexito device configuration. Defaults are in openflexito/config.py.
# host = "0.0.0.0"   # default: all interfaces, IPv4 and IPv6
port = 80
state_dir = "/var/lib/openflexito"
webapp_dir = "/opt/openflexito/webapp"
led_path = "/sys/class/leds/ACT"
hotspot_connection = "openflexito-hotspot"
log_level = "INFO"

[camera]
sensor_size = [3280, 2464]   # full readout, downscaled by the ISP: cleaner preview than the binned mode (as OpenFlexure v3)
stream_size = [820, 616]     # 1640x1232 selectable in the webapp Settings (4x the bandwidth and CPU)
lores_size = [410, 308]
bitrate = 100000000          # JPEG quality cap; actual ~16 Mbit/s at 820x616

[stage]
# port = "/dev/ttyAMA0"      # default: autodetect
backlash = { x = 200, y = 200, z = 200 }
inverted = { x = true, y = false, z = true }
